# 課題1 Variant A: 会員種別変更エッジケース - 講師ガイド

## ゴール

カバレッジ100%でもバグを見逃すテストの問題に気づき、ミューテーションテストを通じてテスト品質を改善する方法を学ぶ。  
**Variant A固有の学び:** PBIに明記された要件（AC-8: 貸出時の会員種別で計算）であっても、AIが正しく実装・テストするとは限らないことを体験する。

## 構成（70分）

1. **ステップ1（1-1〜1-5）**: AIを使ったPBI実装（25分）- 実装とカバレッジ100%達成
2. **ステップ1.6**: エッジケースの動作確認（10分）- 会員種別変更シナリオでバグ発見
3. **ステップ2**: テストの品質検証（15分）- ミューテーションテストで検知率の現実を確認
4. **ステップ3**: テストの改善（15分）- 具体的な期待値で検知率向上
5. **ステップ4**: 振り返り（5分）

## Variant Aの特徴

### PBIの罠

Variant AのPBIにはAC-8が含まれている:

> AC-8: 延滞料金は**貸出時点の会員種別**に基づいて計算する（返却時に会員種別が変更されていても、貸出時の種別を適用する）

**想定される展開:**
- AIはAC-8を読んでも、**現在の会員種別**で計算するコードを生成する可能性が高い
- なぜなら、既存の `LoanService.returnBook()` は `memberRepository` から会員を取得し、その時点の `memberType` を使うため
- AC-8を正しく実装するには、Loanモデルに `memberType` を保存する設計変更が必要だが、AIがそこまで踏み込むかは不確定
- テストも「直接MemberTypeを渡す」形式になるため、この統合レベルのバグを検知できない

### ステップ1.6で発見されるバグ

| 手順 | 操作 | 期待 |
|------|------|------|
| プレミアム会員で貸出 | `memberId:2, bookId:2` | dueDate = 30日後 |
| due_dateを10日前に変更 | SQL UPDATE | 10日延滞状態を再現 |
| 会員種別をGENERALに変更 | PUT /api/members/2 | プレミアム期限切れを想定 |
| 返却 | POST /api/loans/2/return | **期待: 200円**（プレミアム10日×20円） |
| | | **実際: 500円**（一般10日×50円） |

**根本原因:** `LoanService.returnBook()` が返却時点の会員種別を使って計算している。貸出時の会員種別がLoanに保存されていないため、貸出後に会員種別が変わると誤った料金になる。

### 想定されるAIの実装パターン

**パターン1（最も多い）: 返却時の会員種別を使用**
```java
// LoanService.returnBook() 内
Member member = memberRepository.findById(loan.getMemberId())...;
int fee = OverdueFeePolicy.calculateOverdueFee(
    member.getMemberType(),  // ← 返却時の種別を使用（バグ）
    loan.getDueDate(), loan.getReturnDate());
```

**パターン2（稀）: Loanに貸出時の会員種別を保存**
```java
// Loan に memberType フィールドを追加
// borrowBook() で loan.setMemberType(member.getMemberType())
// returnBook() で loan.getMemberType() を使用
```
→ パターン2の場合、AC-8が正しく実装される。この場合はステップ1.6でバグは出ないが、テストの品質問題は依然として存在する。

## 想定される展開

- **カバレッジ100%達成** → 安心感
- **ステップ1.6で会員種別変更バグ発見** → 「PBIに書いてあるのに...」という驚き
- **ミューテーションテストで検知率30%** → さらなる衝撃
- **テスト改善で検知率90%** → 気づき

## よくあるAI生成テストの問題

**パターン1: 存在確認のみ**
```java
assertNotNull(fee);
assertTrue(fee.amount() > 0);  // 50円でも999円でもパス
```

**パターン2: 範囲確認のみ**
```java
assertTrue(fee.amount() <= 1000);  // 800円でも500円でもパス
```

**パターン3: 例外の型のみ**
```java
assertThrows(IllegalArgumentException.class, ...);  // メッセージ未検証
```

**改善後（目指す形）:**
```java
assertEquals(250, fee.amount());  // PBIの計算例を具体的に検証
assertEquals(1000, fee.amount());  // 上限値を具体的に検証
assertTrue(exception.getMessage().contains("返却日"));  // メッセージも検証
```

## ミューテーションテストの検知率（想定）

| バグパターン | 改善前 | 改善後 |
|-------------|-------|-------|
| 定数値変更 (50 → 30) | ✗ | ✓ |
| 境界値条件 (`<=` → `<`) | ✗ | △ |
| 条件分岐反転 (GENERAL ⇔ PREMIUM) | ✗ | ✓ |
| Math.min → Math.max | ✓ | ✓ |
| オフバイワン | ✗ | ✓ |
| 上限値変更 (1000 → 800) | ✗ | ✓ |
| 演算子変更 (`*` → `+`) | ✗ | ✓ |
| 例外メッセージ変更 | ✗ | ✓ |

**検知率: 改善前 30% → 改善後 90%**

## ファシリテーションガイド

### 各ステップでの問いかけ

| ステップ | 問いかけ |
|---------|---------|
| 1-5完了後 | 「カバレッジ100%達成おめでとうございます！では、実際に動かしてみましょう」 |
| 1.6 基本ケース | 「一般会員10日延滞で500円、正しいですね」（安心感を演出） |
| 1.6 種別変更後 | 「佐藤さんの料金はいくらですか？ AC-8を思い出してください」（間を取る） |
| 1.6 バグ発見後 | 「PBIにAC-8として明記されていたのに、なぜこのバグが生まれたのでしょう？」 |
| 2-2結果確認後 | 「検知率30%... カバレッジ100%なのに、なぜでしょう？」 |
| 3-2完了後 | 「検知率が90%に！何が変わったのでしょうか？」 |

### ステップ1.6でバグが出なかった場合

AIがLoanにmemberTypeを保存する設計を選んだ場合、AC-8のバグは出ません。その場合:

1. 「AIが正しく実装しましたね。では、テストコードを確認してみましょう」
2. 「会員種別が途中で変わるケースのテストは書かれていますか？」
3. 「テストがないということは、将来のリファクタリングで壊れる可能性があります」
4. ステップ2のミューテーションテストに進み、テスト品質の問題を体験させる

### トラブルシューティング

**ポート表示されない** → VS Code下部「ポート」タブから38080を確認

**カバレッジ更新されない** → `mvn test jacoco:report` 再実行、ブラウザキャッシュクリア

**バグ元に戻し忘れ** → `git restore <ファイル>` または AIに依頼

**1.6でDB操作がうまくいかない** → PostgreSQLの接続情報を確認（library/library）、テーブル名 `loans` を確認

**PUTエンドポイントがない** → MemberResourceにPUTメソッドがあるか確認。ない場合はSQLで直接変更:
```bash
PGPASSWORD=library psql -U library -d library -c \
  "UPDATE members SET member_type = 'GENERAL' WHERE id = 2;"
```

### 振り返りの質問

1. PBIにAC-8が書かれていたのに、なぜ実装・テストで見逃されたのか？
2. 「存在確認」と「値の検証」の違いは？
3. カバレッジ100%は何を保証する？しない？
4. 統合テストとユニットテストの役割の違いは？
5. AI駆動開発で開発者に求められるスキルは？

## キーメッセージ

**仕様とテストのギャップ**
- PBIに書かれた要件（AC-8）であっても、AIが正しく実装・テストするとは限らない
- 開発者がPBIの要件を理解し、テストでカバーされているか確認する責任がある

**カバレッジ vs 品質**
- カバレッジ100% ≠ テストが機能している
- AIは「行を実行するテスト」を書けるが、「ビジネスルールを検証するテスト」を書くとは限らない

**ミューテーションテストの価値**
- テストの品質を可視化する強力な手法
- 「存在確認」→「値の検証」で検知率30% → 90%

**AI時代の開発者スキル**
- AIが生成したコードとテストの品質を評価・改善できるのは開発者だけ
- PBIの要件をテストに落とし込む能力がより重要に
- 「AIが書いたから大丈夫」ではなく、「仕様通りか自分で確認する」姿勢が必要
