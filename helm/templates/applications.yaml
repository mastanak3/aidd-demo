# App of Apps Pattern
# This template generates ArgoCD Applications for each enabled component

{{- range $name, $config := .Values.components }}
{{- if $config.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Release.Name }}-{{ $name }}
  namespace: {{ $.Values.argocd.namespace }}
  labels:
    demo.redhat.com/application: "field-content"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ $.Values.argocd.project }}
  source:
    repoURL: {{ $.Values.gitops.repoUrl }}
    targetRevision: {{ $.Values.gitops.revision }}
    path: {{ $.Values.gitops.basePath }}/components/operator
    helm:
      valuesObject:
        operator:
          enabled: {{ $config.enabled }}
          name: {{ $config.name }}
          namespace: {{ $config.namespace }}
          channel: {{ $config.channel }}
          source: {{ $config.source }}
          sourceNamespace: {{ $config.sourceNamespace }}
          installPlanApproval: {{ $config.installPlanApproval }}
          {{- if $config.allNamespaces }}
          allNamespaces: {{ $config.allNamespaces }}
          {{- end }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ $config.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{- end }}
{{- end }}


{{- if .Values.isbn_service.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Release.Name }}-isbn-service
  namespace: {{ .Values.argocd.namespace }}
  labels:
    demo.redhat.com/application: "field-content"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.argocd.project }}
  source:
    repoURL: {{ .Values.gitops.repoUrl }}
    targetRevision: {{ .Values.gitops.revision }}
    path: {{ .Values.gitops.basePath }}/components/isbn-service
    helm:
      valuesObject:
        isbn_service:
          enabled: {{ .Values.isbn_service.enabled }}
          name: {{ .Values.isbn_service.name }}
          namespace: {{ .Values.isbn_service.namespace }}
          build:
            gitUri: {{ .Values.isbn_service.build.gitUri }}
            gitRef: {{ .Values.isbn_service.build.gitRef }}
            contextDir: {{ .Values.isbn_service.build.contextDir }}
          port: {{ .Values.isbn_service.port }}
          replicas: {{ .Values.isbn_service.replicas }}
          route:
            enabled: {{ .Values.isbn_service.route.enabled }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.isbn_service.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{- end }}

{{- if .Values.devspaces_image.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Release.Name }}-devspaces-image
  namespace: {{ .Values.argocd.namespace }}
  labels:
    demo.redhat.com/application: "field-content"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.argocd.project }}
  source:
    repoURL: {{ .Values.gitops.repoUrl }}
    targetRevision: {{ .Values.gitops.revision }}
    path: {{ .Values.gitops.basePath }}/components/devspaces-image
    helm:
      valuesObject:
        devspaces_image:
          enabled: {{ .Values.devspaces_image.enabled }}
          name: {{ .Values.devspaces_image.name }}
          namespace: {{ .Values.devspaces_image.namespace }}
          build:
            gitUri: {{ .Values.devspaces_image.build.gitUri }}
            gitRef: {{ .Values.devspaces_image.build.gitRef }}
            contextDir: {{ .Values.devspaces_image.build.contextDir }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.devspaces_image.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{- end }}

{{- if .Values.devspaces_instance.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Release.Name }}-devspaces-instance
  namespace: {{ .Values.argocd.namespace }}
  labels:
    demo.redhat.com/application: "field-content"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.argocd.project }}
  source:
    repoURL: {{ .Values.gitops.repoUrl }}
    targetRevision: {{ .Values.gitops.revision }}
    path: {{ .Values.gitops.basePath }}/components/devspaces-instance
    helm:
      valuesObject:
        devspaces_instance:
          enabled: {{ .Values.devspaces_instance.enabled }}
          name: {{ .Values.devspaces_instance.name }}
          namespace: {{ .Values.devspaces_instance.namespace }}
          devEnvironments:
            {{- toYaml .Values.devspaces_instance.devEnvironments | nindent 12 }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.devspaces_instance.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{- end }}
